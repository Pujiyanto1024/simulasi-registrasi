export interface UniversitasAttributes {
	id: number,
	name: string
}

export interface GenderAttributes {
	id: number,
	name: string
}

export interface ProgrammingLanguageAttributes {
	id: number,
	name: string
}